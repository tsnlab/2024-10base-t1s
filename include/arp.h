#ifndef ARP_H
#define ARP_H

/* TODO: This file is for exposed functions, and should be implemented as below */

/*
 * void t1s_arp_init(uint16_t cache_size);
 * int t1s_arp_send_request(uint8_t* target_ip);
 * int t1s_arp_send_reply(uint8_t* target_ip, uint8_t* target_mac);
 * bool t1s_arp_lookup(uint8_t* ip_addr, uint8_t* mac_addr);
 * void t1s_arp_cleanup();
 */

#endif
